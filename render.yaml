services:
  - type: web
    name: uzhydromet-forecast
    runtime: python
    rootDir: app
    buildCommand: pip install -r requirements.txt && echo "Build v6 $(date)"
    startCommand: gunicorn server:app -c gunicorn.conf.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
      - key: PORT
        value: "10000"
